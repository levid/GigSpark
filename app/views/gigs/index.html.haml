%h1 Listing gigs

%table
  %tr
    %th Title
    %th Date
    %th Booking
    %th Cover
    %th
    %th
    %th

  - @gigs.each do |gig|
    %tr
      %td= gig.title
      %td= gig.date
      %td= gig.booking
      %td= gig.cover
      %td= link_to 'Show', gig
      %td= link_to 'Edit', edit_gig_path(gig)
      %td= link_to 'Destroy', gig, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Gig', new_gig_path
